export enum Tabs {
    Email = 'Email',
    Birthday = 'Birthday',
    Address = 'Address',
    PhoneNumber = 'Phone Number',
    Login = 'Username',
}

export enum TabClasses {
    envelope = 'pi pi-envelope',
    calendar = 'pi pi-calendar',
    map = 'pi pi-map',
    phone = 'pi pi-phone',
    card = 'pi pi-id-card',
}
